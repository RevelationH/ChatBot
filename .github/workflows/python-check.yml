name: python check
on:
  push:
    branches:
      - main
env:
  DEV_PATH: /requirement.txt
 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: python check
        uses: aufdenpunkt/python-safety-check
